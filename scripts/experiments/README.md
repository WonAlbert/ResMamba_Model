# experiments

72h / 6×5090 门控的 YAML overlay **计划**放在 `configs/experiments/`（当前仓库尚未提交该目录）。

启动器：

```bash
python scripts/run_sota_gate.py --dry-run
python scripts/run_sota_gate.py --smoke
```

说明、blocker、六任务指标口径见 [docs/sota_gate.md](../../docs/sota_gate.md)。

**未跑满 72h 门控前不得宣称 SOTA。**
