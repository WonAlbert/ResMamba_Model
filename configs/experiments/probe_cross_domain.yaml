# 可选跨域 frozen probe。缺水声/IMU 数据时门控跳过，禁止在此脚本里下载外部大数据。
_base_: ../stage2.yaml
sota_gate:
  window: D_eval
  job: probe_cross_domain
  optional: true
  requires: [cross_domain_data]
epochs: 2
steps_per_epoch: 100
synthetic: false
